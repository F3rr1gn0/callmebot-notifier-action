# callmebot-notifier GitHub Action

GitHub Action wrapper for `callmebot-notifier`.

## Use

```yaml
- uses: owner/callmebot-notifier-action@v0.1.0
  with:
    message: "Build done"
    channel: "telegram"
```

